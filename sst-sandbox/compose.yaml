services:
  db:
    image: postgres:13-alpine
    environment:
      - TZ=Asia/Tokyo
      - POSTGRES_PASSWORD=postgres
    ports:
      - "5432:5432"
    # volumes:
    #   - db-data:/var/lib/postgresql/data
# volumes:
#   db-data:
